# 2026-09-13

## 答案表按题目原表样式重排（论文排版）

**需求**：用户给出 `D:\09_Temp\C题_表格汇总.tex`（题目表 1–4 的原始 LaTeX），要求把 main.pdf 中「答案所使用的表格」换成该样式。澄清结果：范围＝**仅 5 张"对应题目表 N"的答案表**；版式＝**边框与行列布局一并对齐**。

**改动文件**（仅表格环境，正文一字未动）：
- `sections/04-problem1.tex`：`tab:q1-t1`（表3）、`tab:q1-t2`（表4）
- `sections/06-problem2-results.tex`：`tab:q2-t1`（表8）、`tab:q2-t2`（表9）、`tab:q2-t3`（表10）

**样式对照**：
- 旧：`booktabs` 三线表（`\toprule/\midrule/\bottomrule`），4/3 列窄表 + `\resizebox` 宽表。
- 新：`|c|c|...` 全框线 + 全部 `\hline`；行排布照题目原表——
  - 表1：`时间段|购电量` ×3 并排，两行时段（10/12/14 与 16/18/20），末行「全天购电量｜值｜全天购电费｜值」（标签跨 2 列，值在下一列）。
  - 表2：`时间段|充电量|放电量` ×2，三行时段，末行「0:00 储电量｜值｜24:00 储电量｜值」。
  - 表3：4 个日期组「时间段|购电量」×4 并排 + 日期合并表头 + 3 行数据。
- Q2 含 4 个指定日期，按题目要求（"按表 1、表 2、表 3 的格式给出表 3 中指定日期的结果"）改为**每个日期一个块、纵向堆叠**：表8 21 行、表9 17 行；表10 用表3 的 8 列并排式。
- 单位文本保留：表8/表9 表头不重复带单位（题注已注明），表9 表头用 `\shortstack{紧急购电量\\(kWh)}` 控制列宽，表10 用 `\footnotesize`+`tabcolsep 3pt`+`\resizebox`。
- 表3/表4（Q1）用 `\small`+`tabcolsep 4pt`；表8/表9 用 `\small`+`tabcolsep 4pt`。

**浮动参数选择（踩过一圈）**：
- `[htbp]`：LaTeX 会把 21 行的表8 放成**居中浮动页**（上下约 40% 空白），且**第 45 页只剩一行正文**（"月末的价格效应。"）。
- `[!ht]`：53 页，仍残留第 45 页单行页。
- `[H]`（float 宏包，仅用于表8/表9/表10）：**54 页，无单行页**，第 19 页是含图6 的图页（正常）。**最终采用 `[H]`**。Q1 两张表保持 `[htbp]`（第 11 页排布良好）。
- 试过放宽 `\topfraction` 等（0.92/0.85/0.06/0.88）→ 不但没让表8+表9 同页，还引入 1 处 Overfull，**已回退**，preamble 未改。

**验证**（均通过）：
- `latexmk main.tex`：54 页，**0 Overfull / 0 Underfull / 0 Missing character / 0 LaTeX Warning**。
- 逐页字符数扫描：稀疏页 18/19/22/29/32/54 全部含矢量图（p19 为图6 页），无空白页。
- **文字未改动**：把旧 PDF 所有 ≥25 字符的长行（去空白）逐条在新 PDF 全文（去空白）中查找，**0 条缺失**；反向多出的 5 条全是表格内同一行拼接（如 `16:00–16:10 512.0462 18:00–18:10`），属预期。
- 逐表目视复核：p11（表3、表4）、p20（表8）、p21（表9、表10）渲染正确，数值与旧版一一对应。

**备份**：`_refactor_backup/{04-problem1.tex,06-problem2-results.tex,main.pdf}.0913-1122.bak`。

**副产品**：`sections/*.tex` 中已无 `\multirow` 使用（原仅 tab:q2-t3 用），宏包仍由 sty 加载，无副作用。
