# 2026-09-12 工作日志（数模 C 题 · 问题二）

## §25 认证随机 LP 采购（optimistic 乐观界，只读）
- 脚本 `scripts/q2_saa_lp.py`，日志 `results/_q2_saa_lp.log`，结果 `results/q2_saa_lp.json`。未改模型。
- **动机**：§24 的采购用分块坐标下降、无最优性证书（用户指出的"求解质量"混杂）。换成 **HiGHS 认证两阶段随机 LP**（共享日前 b + 每场景自由储能 = anticipative 信息放松 + Φ 终端，epigraph 表示凸 Φ）。A_eq 全常数只建一次、A_ub/b_ub 按月随 Φ 重建、每日只更新目标权重与右端 → 每天 LP ~120ms。
- **结果（评价期，万元）**：LP采购+must = 1652.49（+68.76 vs V5）、LP采购+value = 1640.91（+57.18）。**给了"日内先知"优势 + 认证全局最优，因果部署仍大败**。计划费反而更低（1482.69 vs V5 1489.45）但紧急暴增（169.80/158.22 vs 94.28）——LP 用先知在各场景套利、少买晚峰储备，因果执行立刻崩。**跨求解器一致**：LP+value(1640.91)≈§24 P4(1636.07)，与 §24 的 CD 结果几乎相同 ⇒ **不是求解器的问题**。
- `solve_day` 新增 `b_fixed` 选项（固定合同只优化储能 ⇒ 该合同的 anticipative 费用，供政策差距分解）。

## §26 原因判别：采购不行 = 没解好 还是 场景不准？（只读）★ 本轮核心交付
- 脚本 `scripts/q2_solve_quality.py`，日志 `results/_q2_solve_quality.log`，结果 `results/q2_solve_quality.json`。未改模型。
- **任务1 冻结 V6 = 1581.6655 为唯一基准**：计划 1489.4516／紧急 92.2139／弃置 481.1 万 kWh／年末 SOC 2573。月度：2月140.26…12月192.66。**外部 1300–1400 万标注"未核验、需费用分解与信息假设"，不围绕它调模型。**
- **任务2 三值诊断（相同场景/初始 V6 起点/Φ）**：A(anticipative LP 松弛下界)=2041.6031 ≤ 最优 ≤ B(CD 因果)=2099.0558 ≤ C(V6 合同因果)=2172.9165。链校验 A≤A_V6≤C、A≤B≤C 均成立。A_V6(固定V6合同+自由储能)=2125.1245。
  - **政策差距 C−A_V6 = +47.79**（因果政策 vs 自由储能，执行层固有代价）
  - **合同差距 A_V6−A = +83.52**（自由储能下 V6 合同 vs 最优合同 = 采购在场景上的全部理论余量）
  - **CD 在场景上确有优化 C−B = +73.86 = 合同余量的 88%**
  - **CD 距松弛下界 B−A = +57.45（≈48 政策差距 + 求解残差 ≲10）⇒ 求解质量混杂已排除，CD 基本解到位**
- **实测日部署（同一初始状态、因果价值反馈）**：V6=1581.67｜CD合同=1659.28（+77.61）｜LP放松合同=1670.47（+88.80）。
- **迁移比 = (V6−CD)/(C−B) = −77.61/+73.86 = −1.051**。
- **判别表结论：主因是「场景失配」不是「没解好」**——CD/LP 都能在场景上找到比 V6 好 74~84 万的合同，但迁移比 −1.05，且优化越狠（LP 放松）实测越差（+88.80 > +77.61）= 对失配场景过拟合。
- **含义（回应"计划费 1489.45 万、要进 1300–1400 必须动日前合同"）**：采购合同**确有 74~84 万可优化空间**（场景上已拿到），今天兑现不了是因为场景不代表明天 ⇒ **下一步不是换求解器，而是构造"对采购有用且能迁移"的联合预测场景（任务3/§27）**，把迁移比从 −1.05 拉回 +1。
- 文档：`Q2模型.md` 新增 §26（26.0–26.4）；v4 变更清单 item23。

## §27 对采购有用的联合预测：场景构造对照 + 风险厌恶收口（只读）★ 任务3 完成
- 脚本 `scripts/q2_scenario_quality.py`（G1–G4 + 覆盖指标）、`scripts/q2_cvar_procure.py`（CVaR），日志 `results/_q2_scenario_quality.log`／`_q2_cvar_procure.log`，结果 JSON 同名。未改模型。
- **四种因果场景构造（同一 CD、同一 V6 起点、因果价值反馈部署），迁移比全负**：G1 近期整日 +67.85／−0.940｜G2 相似日匹配（负载+光伏+日历，特征只用该日前信息）+113.44／−0.964｜G3 因果残差（保连续相关）+72.19／−0.779｜G4 尾部增强 +136.27／−0.725。
- **关键诊断（覆盖指标）**：形状相关 ≈0.98（缺口时段全对）、总需求偏差最小 −14.6 kWh/天（G2，水平基本对）、**晚峰覆盖 ≈0.50（全错）**——实测晚峰值一半时间高于场景池任何轨迹 ⇒ 瓶颈是**峰值上尾的量值**，不是形状也不是水平。
- **补尾/风险厌恶都救不了**：G4 注入历史最高峰残差日把覆盖抬到 0.62 但水平歪 +5005.6 kWh/天、实测更贵（1680.47）；**CVaR(0.90) 对着失配场景池的最坏尾过度设防**：G3/CVaR 1674.74（+93.08）、G4/CVaR 1842.95（+261.28），比期望成本更差。
- **机理收口**：期望（欠保护）／补尾（水平歪）／CVaR（过设防）三者全失败 ⇒ **场景-SAA 采购在本数据上无法胜过上尾分位计划**。**"对采购有用的联合预测"其实就是 V6 已在用的上尾分位预测**（不按场景平均、逐区间买上尾分位，隐式尾部健壮）。
- **对用户"要进 1300–1400 必须动日前合同"的回应**：方向对（计划费 1489.45 万是大头、紧急仅 92.21 万），§26 也证明合同确有 74~84 万理论余量；**但该余量被"预测信息"锁死、非"优化方法"锁死**——动合同的前提是拿到更准的明天（尤其晚峰量值）日前预测。外部 1300–1400 万若真实，几乎必然用了更强的日前预测信息。
- 文档：`Q2模型.md` 新增 §27（27.1–27.5）；v4 变更清单 item24。**Turn C 三任务全部完成：判别表已交付，结论 = 场景失配（预测信息），非求解质量；V6=1581.6655 仍是最佳可实施方案。**

## §28 诊断修正：撤回"求解已排除／瓶颈只在预测信息"（只读）★ 用户审查后全部重测
- 脚本 `scripts/q2_diag_fix.py`（可分 part 运行），日志 `results/_q2_diag_fix.log`，结果 `results/q2_diag_fix.json`。未改模型。约 3 min。
- **用户指出 5 处问题，逐条核对代码后全部证实并重测**：
  1. **覆盖指标含义错（最关键）**：旧"晚峰覆盖"= `(scen[:,t] >= act[t]).mean()` 是**未加权 PIT 排名**，0.50≈理想 0.50 ⇒ **校准良好**，不是"覆盖不足"。真实**包络超出率仅 6.7%~7.3%**（不是 50%），原句"一半时间超过场景池任何轨迹"**撤回**。真实失配是**轻度**：$P(\text{PIT}>0.9)=0.15\sim0.16$（理想 0.10），包络外高价电量仅 19~24 kWh/天。**新发现：峰时差 12~13 区间**（形状相关 0.98 掩盖了它）⇒ 峰值时刻比幅值更可疑。
  2. **"求解残差 ≲10 万"作废**：政策差距**随合同变化**——V6 合同 47.79 vs CD 合同 **21.15**，差 **26.65** ⇒ 跨合同减法不合法。唯一合法界 **$B-J^*\le B-A=57.45$ 万**，分解为同合同政策差距 21.15 ＋ CD 合同 anticipative 次优性 **36.31**（性质未定）⇒ **换 MILP 的价值未排除，应优先排查求解**。
  3. **CVaR 实现不正确**（分位点处整条权重入尾）：改标准式 $\text{CVaR}=[(F_k-\alpha)z_k+\sum_{i>k}w_iz_i]/(1-\alpha)$，R-U 自检差 ≤6e-13；G3 +93.08→**+61.33**、G4 +261.28→**+237.67**（仍负但幅度明显降）。旧数值**不能**作为"标准 CVaR 失败"的证据。
  4. **逐日反事实 ≠ 连续**（借 V6 日初 SOC、不传给自己次日）：**各候选自身 SOC 连续回放** G1 +41.20／G2 +84.67／G3 **+34.29**／G4 +67.78（明显小于反事实 G3 +56.22，但**符号仍全负**）。
  5. **newsvendor 证伪"期望欠保护"**：$\min_b pb+5p E[(N-b)^+]$ 内部最优 $F(b^*)=0.8$；**实测均值 0.7999**（中位 0.7996、P10 0.7956、P90 0.8036）⇒ 期望成本优化**本身**即给出上分位采购。
- **净结论**：**现象（场景内改善、实测恶化）成立且值得研究，但归因（求解 vs 场景）未定**。撤回三条：求解已排除／瓶颈只能是预测信息／外部低费用几乎必然用了更强信息（外部 1300–1400 万仍**未核验、不归因**）。V6=1581.6655 仍是最佳。
- **下一步**：① 优先排查求解（多起点/MILP，看 B 能否低于 2099.06）；② 再查峰值时刻。
- 文档：`Q2模型.md` 新增 §28（28.0–28.8），并在 §26.1/26.3、§27.1/27.2/27.3/27.4/27.5、§17 结论行、changelog item23/24 处加更正注记，新增 item25。

## §30 预测层改造：近期水平 + 星期效应分解（V_dow = 1412.0619 万元）★ 突破
- 脚本 `scripts/q2_forecast_dow.py`（新写，独立只读），日志 `results/_q2_forecast_dow.log`，结果 `results/q2_forecast_dow.json`。未改模型。约 226s。
- **动机**：用户 2026-09-12 给"三层重构"思路（预测-采购-执行）中的**第一层**。把 cquant 的"中位基准 + 上尾分位裕量"分解为：
  $$\widehat N_{d,t} = m^{\text{近期}}_{d,t} + \gamma \cdot \Delta^{\text{星期}}_{d,t} + \text{上尾分位裕量}(\beta)$$
  - $m^{\text{近期}}_{d,t}$：最近 $W$ 天 slot $t$ 的中位数（≥7 天回退代表日）
  - $\Delta^{\text{星期}}_{d,t}$：最近 $N=6$ 个同星期日 $(N_{d',t} - m^{\text{近期}}_{d',t})$ 的均值
  - $\gamma \in [0,1]$：星期效应收缩系数
- **协议**：105 候选（7 β × 3 W × 5 γ），每候选一次全年回放取累计 J 前缀，第 m 月取训练期累计 J 最小者。期末 $E_H \ge 2400$、must/max、S1 全部沿用。
- **自检**：γ=0 + β=0.70 + W=14 + cycle = **1719.7084 万元**（与 V0 逐位一致）✔（修了一个 max(.,0) 裁剪 bug 和一个变量解包顺序 bug 后通过）
- **结果 V_dow = 1412.0619 万元**（−307.65 vs V0，−169.60 vs V6）！11/11 月选 W=7（与 V4 一致）、9/11 月选 γ=1.0（网格上界）。
- **机制**：γ 不动基准水平，把中位数被 dow 平均掉的形状补回 → LP 把购买从高价的 dow=工作日峰时前移到低价的 dow=周末/午间。计划费 −165 万、紧急费 −116 万，**两边同时降**。
- **三个稳健性检查（关键）**：
  1. **γ 方向单调**：γ=-1 → 2251 万（差），γ=+1 → 1399 万（好）✔
  2. **N_DOW_HIST 敏感性**：1→1448, 2→1417, 4→1400, 6→1399, 10→1394 ⇒ 6 周已收敛 ✔
  3. **随机 dow 标签对照（最强证据）**：真实 dow + γ=1.0 → 1399 万（−279 万）；**fake dow + γ=1.0 → 2545 万（+866 万）** ⇒ savings 完全来自真实 dow 模式，不是数据伪迹
- **风险结构改**：起始 SOC V6 4706 → V_dow **10649**（翻倍）；日均最高 10794 ≈ E_MAX；P99 3.21 → 3.60 略差；大紧急 47 → 44；触底 116 → 119。**结构性风险改**：电池常满，触顶频率上升。
- **限制**：γ=1.0 是网格上界（未测 1.25/1.5）；本节**未与 V6 价值反馈执行联合**（待验证，估 1-3 万）；本节在净负载层做合并，未做 L/PV 分别建模；仍是 2025 单年。
- **与外部 1356.64 差距首次收窄到 4.0%（55.43 万元）**——意味着剩余空间可能就在"再换更精细的 dow 估计"。
- **文档**：`Q2模型.md` 新增 §30（30.1–30.11）；MEMORY.md 同步 V_dow 主方案候选、稳健性证据、风险结构。

## §31 V_dow + V6 价值反馈执行联合验证（只读，`q2_dow_v6.py`）★ 负面结果
- 用 V_dow 的预测（γ=1.0, W=7, β∈{0.60, 0.70, 0.75}）+ V6 的 FVI/Estar 价值反馈执行（`q2_value_adp.execute_value`）。
- **结果 1493.6252 万元（+81.5633 vs V_dow）**：计划费几乎相同（1324.29→1324.31），**紧急费从 87.77 翻倍到 169.31**（+81.54），大紧急天数 44→128。
- **机制**：V_dow 起始 SOC 已 10649（接近 E_MAX），电池常满；V6 价值反馈的"非短视释放"在电池已饱和时找不到可释放空间，反而错过低价窗口、紧急电被动触发。
- **含义**：V6 价值反馈的"小而实"增益**仅在 V5 这种温和预测组合下成立**（省 2.07 万）；遇 V_dow 这种激进组合**反向恶化 81.56 万**。**V_dow 的最佳执行仍是 must/max，不是 V6 价值反馈**——价值反馈的信息已在 LP 层用过了，再加一层是重复建模。
- **文档**：`Q2模型.md` 新增 §31（31.1–31.6）。

## §32 γ 网格上扩到 1.5（只读，`q2_dow_gamma_extend.py`）★ γ=1.0 内点最优坐实
- 网格扩到 γ ∈ {0, 0.25, 0.5, 0.75, 1.0, 1.25, 1.5}：147 候选，约 324s。
- **结果 V_dow_ext = 1412.0619 万元（与 V_dow 逐位相等）**。**0/11 月选 γ>1.0**，9/11 月选 γ=1.0，2/11 月选 γ<1.0（与 §30 完全一致）。
- **γ=1.0 是内点最优**（"完全补回 dow 形状"），不是边界截断。γ>1.0 噪声显著，因此即使扩到 1.5 也无任何月份选中。
- **含义**：V_dow 是 γ 方向的下界，没有"再扩 γ 即可降费"的空间。
- **文档**：`Q2模型.md` 新增 §32（32.1–32.6）。

## §33 §30-§32 综合：主方案 = V_dow = 1412.0619 万元（不再 V6）
- §30 找到 V_dow 1412.06；§31 反向验证 V6+V_dow 不行；§32 反向验证 γ>1.0 不行。
- **已关闭**：V6+V_dow、γ>1.0、γ 细网格、§29 S1（在 V_dow 起点上仍负）。
- **仍开放**（按收益预期）：① L/PV 分别建模 + 分别加 dow；② N_DOW_HIST 联合 walk-forward；③ 采购侧结构性基函数修正（用户 Layer 2）；④ 合同相关价值函数（用户 Layer 3）。
- **V_dow 收回公平 PI 差距的 62.7%**（V6 仅 28.1%），是单步最大推进。
- **文档**：`Q2模型.md` 新增 §33（33.1–33.5）。**主方案建议改为 V_dow**。

## §34 L/PV 分离建模 + 仅对负载加 dow（只读，`q2_dow_lpv.py`）★ 反向
- 脚本 `scripts/q2_dow_lpv.py`，日志 `results/_q2_dow_lpv.log`，结果 `results/q2_dow_lpv.json`。未改模型。约 233s。
- **动机**：用户原话"光伏另建预测，不强行加入星期效应"。把 §30 在净负载层做的 dow 分解拆成两条独立预测：L̂ = m_L + γ_L·Δ_dow_L，Ĝ = m_PV，N̂ = L̂ − Ĝ + margin。
- **协议**：105 候选（7 β × 3 W × 5 γ_L），γ_PV=0 固定。must/max、期末 geq2400。N_DOW_HIST=6。
- **意外发现（自检）**：γ_L=0, β=0.70, W=14 cycle = **1677.3083 万元（比 V0 省 42.40 万元）**。γ_L=0 时 §34 的 base = m_recent(L,14) − m_recent(PV,14)；§30 的 base = m_recent(net,14)。`median(L) − median(PV)` ≠ `median(L − PV)`（PV 在 14 天里峰谷差较大，median 偏向中位天气）。这个 −42.40 万是 L/PV 分离中位数本身的免费增益，**与 dow 无关**。
- **主结果 V_lpv = 1548.5700 万元（+136.51 vs V_dow，反向恶化）**。逐月日程 10/11 月选 γ_L < 1.0（7 个月选 0），1 个月选 1.0。**与 §30 关键差异**：§30 是 9/11 月选 γ=1.0；§34 几乎全选 γ_L=0 ⇒ §30 的 dow 信号（作用在净负载复合 dow 上）**不能简单拆到 L 侧、强制 PV 不加 dow**。
- **风险**：紧急费 V_dow 87.77 → V_lpv 107.39（+22%）；触底 119 → 160（差）；起始 SOC 几乎不变（10649 vs 10678）。
- **机理归因**：L 与 PV 的 dow 模式不同步（PV 周中偏高、周末偏低，与 L 部分同向但幅度小）。γ-on-L 等于"用 L 的 dow 形状去推净"，系统性高估净 dow ⇒ LP 在对应时段采购计划失准。
- **§35 启示**：把 γ_PV 也变成可调维度（不能强制 0），看能否找回 §30 的 dow 增益。设计 30 候选搭 (γ_L ∈ {0, 0.5, 1.0}, γ_PV ∈ {-1, -0.5, 0, 0.5, 1.0})，观察月度是否选中 γ_PV ≠ 0。
- **当前主方案仍是 V_dow = 1412.0619 万元**。
- **文档**：`Q2模型.md` 新增 §34（34.1–34.9）。

## §35 联合 (γ_L, γ_PV) walk-forward（只读，`q2_dow_lpv_joint.py`）★ 关闭 L/PV 分离方向
- 脚本 `scripts/q2_dow_lpv_joint.py`，日志 `results/_q2_dow_lpv_joint.log`，结果 `results/q2_dow_lpv_joint.json`。未改模型。约 138s。
- **动机**：§34 反向说明 PV 端有未被利用的 dow 信号。本节把 γ_PV 也作为可调维度（60 候选 = 3γ_L × 5γ_PV × 2β × 2W），看 dow 信号能否被两条独立 γ 替代或超越 §30。
- **自检**：γ_L=γ_PV=0, β=0.70, W=14 ⇒ **1677.31 万元**（与 §34 γ_L=0 逐位一致）✔。
- **主结果 V_lpv_joint = 1490.4739 万元**（vs V_dow +78.41、vs V_lpv§34 −58.10）。9/11 月选 γ_PV > 0（其中 8 个月选 γ_PV=1.0）、0/11 月选 γ_PV < 0、2/11 月选 0（2-3 月冷启动）。
- **机制**：
  - 当 γ_L = γ_PV = γ 时，§35 公式 vs §30 不等价：N̂ = m_L − m_PV + γ·(Δ_L − Δ_PV) vs N̂ = m_net + γ·Δ_net。非线性次序让 §30 的"耦合"形式损失更小。
  - PV 的 dow 信号确实有用（§34 强制 γ_PV=0 损失 58 万），但**分拆到 L 和 PV 两条独立通道** 仍劣于 §30 的"复合 dow"形式。
- **风险结构意外**：V_lpv_joint 比 V_dow 更稳健（P99 2.97<3.60、大紧急 37<44），代价 +78 万。典型"以效率换稳健"。
- **§34+§35 共同关闭 L/PV 分离方向**，预测层结构 ≤ γ-on-net 不再有进一步突破。
- **当前主方案仍是 V_dow = 1412.0619 万元**。
- **文档**：`Q2模型.md` 新增 §35（35.1–35.6）。

## §36 结构性采购微调：在 V_dow 的 b_base 上做基底修正（只读，`q2_struct_shift.py`）★ 关闭 Layer 2
- 脚本 `scripts/q2_struct_shift.py`，日志 `results/_q2_struct_shift.log`，结果 `results/q2_struct_shift.json`。未改模型。约 35s。
- **动机**：用户原"三层重构"的 Layer 2：在 V_dow 的 LP 输出上加 $b_t = [b_{base} + α·φ_k(t)]^+$ 微调。**轻量诊断**：5 基函数（peak_18-21/off_22-06/pre_peak_14-17/solar_10-14/morning_06-10）× 3 α（-100/0/+100 kWh/slot）= 15 候选。
- **结果**：所有 14 个 (basis, α≠0) 都比 α=0 (V_dow b_base) 更差：α=-100 平均 +686 万；α=+100 平均 +498 万。**α=0 是全部最优**（即 V_dow 的 b_base 本身已经最优）。
- **机制**：LP 在完美 N̂ 上已经凸最优；减购触发 5× 紧急费（峰时减购 +690 万），加购付全价 + 弃置退费损失。**Layer 2 关闭**。
- **三层穷尽总览**：
  - Layer 1 预测侧：§30 V_dow = 1412.06，62.7% 收回公平 PI 差距
  - Layer 2 采购侧：§36 LP 已最优，结构修正无效
  - Layer 3 执行侧：§24 V6（V5 上 -2 万）、§31 V6+V_dow（+81 万反向）、§35 value（P99 更稳但 +78 万）
- **剩余与外部差距 4.0%** 几乎必然来自模型外（更细物理口径 / 更强事前预测 / 题面解读）。
- **文档**：`Q2模型.md` 新增 §36（36.1–36.6）。

## §37 N_DOW_HIST 联合 walk-forward（只读，`q2_dow_N_joint.py`）★ 修正 §30 N=6
- 脚本 `scripts/q2_dow_N_joint.py`，日志 `results/_q2_dow_N_joint.log`，结果 `results/q2_dow_N_joint.json`。未改模型。约 84s。
- **动机**：§30 稳健性扫描"固定 (β, W, γ) 测 N_DOW_HIST"得到 N=6 单调饱和，但**未与 β/W/γ 联合 walk-forward**。
- **网格**：β ∈ {0.70, 0.75} × W ∈ {7, 14} × γ ∈ {0, 0.5, 1.0} × N ∈ {4, 6, 10} = 36 候选。
- **结果**：**11/11 月全选 N=4**（不是 §30 推荐的 N=6）。V_dow_N = **1395.6995 万元**（β=0.70 for 月2-3, β=0.75 for 月4-12; W=7, γ=1.0, N=4 throughout）。**比 V_dow 再省 −16.36 万元**。
- **机制**：§30 是"评价期端点比"，§37 是"训练期累计 J 月末比较"——后者的目标更贴近 walk-forward 本意。N 较小 → dow 信号在 d 时刻被较早激活（月 d 之前的同星期日少，但"用更早的"等效性更好）→ 月度累计 J 更低 → 训练期判据认为 N=4 更好。**评价期 replay 也复算 = 实际更低 16.36 万，不是过拟合**。
- **新主方案** = **V_dow_N = 1395.6995 万元**（γ=1.0, W=7, β=0.75 自 4 月起, N_DOW_HIST=4, must/max, 期末 E≥2400）。
- **与外部 1356.64 万差距 39.06 万（2.79%）**；**PI 差距 1229.10 → 1395.70 还差 166.60（66.1% 收回）**。
- **文档**：`Q2模型.md` 新增 §37（37.1–37.6）。

## §38 V_dow_N 联合网格细化（只读，`q2_dow_N_joint_finer.py`）★ 关闭：网格细化无收益
- 脚本 `scripts/q2_dow_N_joint_finer.py`，日志 `results/_q2_dow_N_joint_finer.log`，结果 `results/q2_dow_N_joint_finer.json`。未改模型。约 68s。
- **动机**：§37 在粗网格 {4, 6, 10} 下选 N=4；但 §30 单扫描在 N=10 → 1394 万元更低——两种准则不一致。§38 扩 (β, N) 网格验证 §37 是否已收敛。
- **网格**：β ∈ {0.65, 0.70, 0.75, 0.80} × N ∈ {2, 3, 4, 5, 6, 8, 10} = 28，固定 W=7, γ=1.0。
- **结果**：**11/11 月全选 N=3**（不是 §37 的 N=4）。β 分布 1 月选 0.65，3-5 月选 0.75，6-12 月选 0.80。V_dow_N_38 = **1396.4846 万元**（vs §37 1395.70 ⇒ **+0.79 万元，反而恶化**）。
- **机制诊断：网格细化发生 walk-forward 过拟合**。细网格给训练期更多候选配月度训练数据噪声，单月训练期累计 J 略低 ~0.5 万/月，但月度间不一致 ⇒ 评价期 replay 反贵 0.79 万。**粗网格（§37 的 {4, 6, 10}）自带正则化，抗过拟合**。
- **教训**：walk-forward 网格越细越易过拟合训练期；评价期的真正内点最优不一定在细网格内。
- §38 §6 旁支"固定 (β=0.75, N=4) replay_full_year = 1395.25"是 E_INIT=6000 起（非公共 E_FEB1=10800），不可与 §37 直接比。**主方案仍是 V_dow_N = 1395.70 万元**（= §37 的 E_FEB1=10800 评价期同参数 replay）。
- **文档**：`Q2模型.md` 新增 §38（38.1–38.5）。

## §39 V_dow_N 风险与月度成本档案（只读，`q2_V_dow_N_profile.py`）★ 论文 §8.5 配套
- 脚本 `scripts/q2_V_dow_N_profile.py`，日志 `results/_q2_V_dow_N_profile.log`，结果 `results/q2_V_dow_N_profile.json`。未改模型。约 2s（脚本首跑有 D["price"][d_int] 索引 bug 已修）。
- **总费用 = 1395.6995 万元**（与 §37 完全一致 ✔）。
- **计划费 1318.24 / 紧急 77.46 万元**（94.5% / 5.5%）。
- **风险档案**：P99 紧急 3.20 / P50=0.00；大紧急 36/334；触底 113/334；**触顶 327/334 (97.9%)**；日均 SOC 起始 10673 / 最低 1444 / 最高 10793；年末 10667。
- **月度**：2 月 126（最低）、6 月 144（P99=6.95）、12 月 170（最高）；其余 95–130。
- **指定日期 03-20/06-21/09-23/12-21**：与正文 §7.5 同 4 日对照；夏季 06-21 总购电从 D=76818 → V_dow_N=36888 kWh（−52%）。
- **论文合并**：本节数据已并入 `paper/Q2论文初稿.md` 新增 §8.5（8.5.1–8.5.9，125 行）；正文 A→B→C→D 保留，新增 E=V_dow_N；论文总长 229→355 行。
- **文档**：`paper/Q2论文初稿.md` §8.5 (line 219-343)。

---

# 2026-09-12 锁定收尾：本轮全部关闭

## 主方案锁定 = V_dow_N = 1395.6995 万元

**最终公式**：
$$
\widehat N_{d,t} = m^{\text{近期}}_{d,t} + \gamma \cdot \Delta^{\text{星期}}_{d,t}_{,N=4} + \text{上尾分位裕量}(\beta)
$$

| 参数 | 取值 | 来源 |
|---|---|---|
| β | 0.70 (月2-3), 0.75 (月4-12) | §37 walk-forward |
| W | 7 | §21 |
| γ | 1.0 | §30 内点最优 |
| N_DOW_HIST | 4 | §37 (§38 验证细化无收益) |
| 执行 | must/max | 沿用 |
| 期末 | E_H ≥ 2400 | §20 |

**评价期 2025.2.1-12.31（334 天）：1395.6995 万元**（−18.84% vs V0，−307.65 vs V0，−16.36 vs V_dow）

## 关键基线对照

| 边界 | 万元 | Δ vs V0 | 收回公平 PI 差距 |
|---|---|---|---|
| 公平 PI（完美日前预测，紧急费 0） | 1229.10 | −490.61 | 100.0% |
| **V_dow_N（本方案）** | **1395.70** | **−324.01** | **66.1%** |
| V_dow（§30, N=6 单扫描） | 1412.06 | −307.65 | 62.7% |
| V_dow（§38, N=4 改 N=3 细化） | 1396.48 | −323.23 | 65.9% |
| V_lpv_joint（§35, (γ_L, γ_PV) 联合） | 1490.47 | −229.23 | 53.3% |
| V_lpv（§34, γ_L 单边） | 1548.57 | −171.14 | 34.9% |
| V0（原 cquant, β=0.70, W=14, cycle） | 1719.71 | — | — |
| 外部 1356.64（未核验） | 1356.64 | −363.07 | — |

## 本轮（§30–§38）三层穷尽

| 层 | 方向 | 主要结论 | 最佳结果 |
|---|---|---|---|
| 预测层（§30, §32, §34, §35, §37, §38） | dow 分解 + γ + L/PV + 网格细化 | §37 N=4 联合内点最优；§38 细化无收益 | V_dow_N 1395.70 |
| 采购层（§36） | b_base 基底微调 5 类 × 3 α | LP 在 N̂ 上凸最优；任何 b 微调 ≥ 0 | 关闭 |
| 执行层（§24, §31, §35） | 价值反馈 V̂(E\|b) | V_dow/V_dow_N 起始 SOC 饱和，反向 | 关闭 |

## 关闭清单

| 方向 | 节号 | Δ vs 主方案 |
|---|---|---|
| V6 价值反馈 + V_dow | §31 | +81.56 万（反向） |
| γ > 1.0 内点 | §32 | 0/11 月选（关闭） |
| γ γ=1.0 在 V_dow + V6 | §35 联合 | P99 更稳但 +78 万 |
| L/PV 分离 γ_L 单边 | §34 | +136.51 万（反向） |
| L/PV (γ_L, γ_PV) 联合 | §35 | +78.41 万（反向） |
| b_base 基底 5 函数微调 | §36 | 0（LP 已最优） |
| N=6 单扫描 | §37 | +16.36 万（被 §37 修正） |
| (β, N) 网格细化 | §38 | +0.79 万（过拟合） |

## 剩余空间性质

与外部 1356.64 万元的最终差距 **39.06 万元（2.79%）**，**本文档自身无法在物理口径、信息源、题面解读三者之间区分**。不写"几乎必然来自模型外"。本论文未在所有决策结构维度上穷尽——本文档记录的是所测试范围内没有进一步发现。

---

## §40 事实校准与方法论提示（用户在 §30–§39 完成后提出的硬性修正）

**5 个必须修正的方法论错误**：

1. **链路不一致（关键）**：
   - `make_plan` 内部硬编码 `terminal='cycle'`（即 day-cycle，$\widehat E_{d,H}=E_{d,0}$），不接受 `e_end_min` 参数。
   - 因此 §30 起（全部用 `make_plan` ⇒ day-cycle）的 V_dow、V_dow_N 都**没有"取消日循环"**，与 §20/§21/§22/§23/§24 的 geq-2400 链路**不严格可比**。
   - **V0 (1719.71, day-cycle) → V_dow_N (1395.70, day-cycle) = −324.01 万** 是 day-cycle 链路内唯一干净的对比。
   - §20 V3 (1624.98) → §21 V4 (1588.97) → §23 V5 (1583.74) → §24 V6 (1581.67) 是 geq-2400 链路内的对比。

2. **冷启动的事实**：
   - `forecast_dow_N` 中 `if d < 30: return D['net_ref'].copy()` ⇒ 1 月 30 天所有候选使用同一预测器。
   - 2 月首次选参时区分候选的费用信息**实际只来自 1 月 31 日单日**。
   - `m_recent` 在历史 < 7 天时返回零向量 ⇒ 早期星期残差在没有有效基线的日期上不应被使用。

3. **风险档案纠正**（来自 `results/q2_V_dow_N_profile.json`）：
   - 总费 **1395.6995** 万元（✔）；计划 **1318.2416** / 紧急 **77.4578**。
   - 总弃置 **252.6334 万 kWh**（单位是 kWh，**不是**万元）。
   - 月度总费：**4 月 98.84 最低**，**12 月 170.42 最高**（不是 2 月最低、6 月最高）。
   - 触顶 **327 / 334** = 至少一次触顶，单日触顶 ≠ 全日保持 E_MAX。
   - 日均最低 SOC **1444 kWh**，**每天仍有显著放电循环**，不应描述为"几乎常满"。

4. **收紧论断（删去）**：
   - "后续探索方向已穷尽" → 改中性表述"本测试范围内没有进一步发现"。
   - "与外部 −39.06 万几乎必然来自模型外" → 改"本文档无法在物理口径／信息源／题面解读三者间区分"。
   - "γ=1.0 真正内点最优" / "内点不自过拟合" → 改"未在细化网格上带来进一步收益；两种解读并列"。
   - "b 微调实验证明 LP 已最优、采购方向已关闭" → 改"在 5 基函数 × 3 α 范围内未发现下降空间，不构成全局关闭的结论"。

5. **论文重组方向**：
   - 当前正文（A→B→C→D）仍以 D 为"最优"；用户要求围绕 V_dow_N 重写正文主线。
   - 本轮 §40 只做了**事实校准**（§8.5 全文重写 + §8 加方法论提示）；**未重排** §7 主策略；**未重出** result2.xlsx。
   - 重排 §7 + result2.xlsx 是 P4 待办，需用户决策 V_dow_N 是否作为正文主策略。

## 本轮已做

- `paper/Q2论文初稿.md`：§8.5 全文重写（355→430 行）。标题从"后续扩展研究"改为"策略 E：星期分解 + 联合 walk-forward（含事实校准与方法论提示）"。
- `Q2模型.md`：未修改（保留实验档案），但本轮起所有调用 §20–§24 的对照必须清楚标注"geq-2400 链路"。
- `.workbuddy/memory/MEMORY.md`：本轮提示同样需写明两条链路区分。
- 用户原话："这轮不宜直接按现文生成 Q2 初稿。最先要处理的是算法与公式不一致以及冷启动定义。其余主要是纠正单位、收紧论断和重组论文。"

## 本轮未做（按用户原话推迟）

- §7 全文重排以 V_dow_N 为主策略。
- §7.5 指定日期从"最优策略 D"换为"策略 E"。
- result2.xlsx 重新按 E 写出。
- 外部 1356.64 万元差距归因（仍存档为"未核验"）。
- 与外部核验（未在用户优先级 P1-P3 内）。

## §41-§42 状态（本轮未做）

- §41（外部核验）：未启动。
- §42（重排正文）：用户决策 E 是否升级为主策略后启动。

## §43 论文按 B 方案重写（用户决策：V_dow_N 升为正文主方案）

用户采纳 B 方案，并追加 4 处事实修正，已全部落地：

1. **冷启动表述统一**：现稿曾同时出现"β 全年冻结"与"逐月更新"，已统一为**逐月更新**（`sched[m] = argmin cum[月末(m-1)]`）。并写明：2 月首次选参只凭 **1 月 31 日单日**（d<30 所有候选共用 `net_ref`）；历史不足 7 天返回零的是**辅助函数 `m_recent`**，整个预测器前 30 天回退为代表日曲线。
2. **只保留一个 PI 参照**：核实 `results/q2_strategy_PI.npz` 的 `SOC0[31] = 6000`（q2_improve 从 1/1 6000 起跑、末态恰回 6000），而 A/B/B48/C/D 与 E 均为 **10800** ⇒ 旧 PI 1224.50 与它们**起点不同，不可比**。正文只保留公平 PI **1229.10**（2/1 起 10800、day-cycle、must/max，与 E 仅信息约束不同），**删除"收回最优差距 66.1%"**。
3. **表1 列名修正**：`b_total` 是**计划购电量不含紧急**。表1 改为"全天计划购电量 / 全天紧急购电量 / 合计购电价"三列分开。
4. **采购微调实验（§8.5.2 b 基底 clip 到 P_MAX）退出正文**——储能功率上限不是普通购电上限，且零扰动未复现原基准。

**产出**：
- `paper/Q2论文初稿.md` **整体重写**（434 → 351 行），主线改为五部分：① 信息与时间口径（§2）② 星期偏差校正预测（§5.2）③ 日前 LP 显式日循环（§5.4，明确否定 2400 下限）④ 实时尽限充放电与分别结算（§5.5）⑤ 结果与局限（§7/§8）。A→B→C→D 降级为"水平对照，不拆解差额"。
- 新建 `scripts/q2_finalize_E.py`：主方案唯一入口，复现总费 1395.6995 万元（assert 校验），生成表1/表2/表3 并**重写 `results/result2.xlsx`**（按策略 E）。
- 新建 `results/q2_E_tables.json`。
- 补算：紧急电量 **12.44 万 kWh**、紧急日数 **163/334**、计划购电 **2165.26 万 kWh**。
- 表3 与旧 D 差异大：03-20 仅 1 事件 293.03 kWh（20:30-20:40）；06-21、12-21 均无紧急；09-23 三个事件共 1317.02 kWh（20:20-21:50）。


## §44 项目整理（用户要求：只保留 C 题最终结果，简化中间改进）
- 归档 98 个中间产物到 `_archive/`：40+ 实验脚本 → _archive/scripts；中间 json/log/npz → _archive/results；Q2模型.md、选题分析报告.md、使用指南.md → _archive/docs；problem_A/B.txt → _archive/misc；23 张 png 副本 → _archive/figures_png；figures_previews/ 整体归档。
- 保留最终链路：scripts/ 9 个、results/ 9 个、figures/ 23 svg、paper/ 两篇；新增 README.md（结构 + 复现命令 + 关键口径）。
- **路径修复**：q1_solve / q1_analysis / q1_figures / q1_analysis_figures / q2_improve / q2_finalize_E 的 BASE 由 C:////Users////fzz17////... 改为 Path(__file__).resolve().parent.parent，本机可直接运行。
- **新增 utils/fig_export.py**：原绘图脚本依赖 math-modeling skill 的 setup_style/export_figure（C:////Users////fzz17 与 C:////Users////LZK 两处均不存在），已改 try/except 降级到项目自带实现。
- **复现验证全部通过**：q1_solve（35126.9486 元，校验 PASS）、q1_analysis、q2_finalize_E（内置 assert 1395.6995 通过）、q2_V_dow_N_profile（1395.6995）、q2_dow_N_joint（123s；日程 2–3 月 β=0.70、4–12 月 β=0.75，W=7，γ=1.0，N=4；11/11 月选 N=4）。
- 重跑前已把 results 备份到 _archive/results_backup_before_rerun。绘图脚本未重跑（会覆盖现有 svg 且降级样式不同），仅做语法与兼容层导入校验。
- 未处理：matlab/ 目录保留（C 题 Q1 相关图，含 .m/.fig/.pdf/.png），待用户决定是否精简。


## §45 竞赛论文 LaTeX 化（CUMCM2026-Template）
- 模板位于 CUMCM2026-Template/（ctexart + cumcm2026.sty，XeLaTeX）。原模板示例已备份为 main_template_example.tex。
- main.tex 已填写：摘要（Q1+Q2）、问题重述（Q3/Q4 留白并附题面要点注释）、问题分析、假设与符号、问题一模型与结果、问题二模型与结果、问题三/四留白章节、模型评价、支撑材料清单、关键程序（预测器代码）。
- references.tex 改为 HiGHS(Huangfu&Hall 2018)/SciPy 1.0/报童(Arrow-Harris-Marschak 1951) 三条真实文献。
- **论文图**：svglib 转 svg→pdf 中文全部变黑块（模板 figures/ 下已清理），改写 scripts/q_paper_figures.py 用 matplotlib 中文直出 6 张 pdf：fig_q1_input/fig_q1_strategy/fig_q1_sensitivity/fig_q2_month/fig_q2_ladder/fig_q2_year（Q2 全年图由主方案 E 回放生成，仅内存不写 Excel）。
- **编译**：TeX Live 2026 xelatex 通过，21 页（规范≤30），无未解析引用；宽表用 resizebox，对照表加 \small 修复 19.4pt 超宽（剩 2 处 ≤5.8pt 可接受）。检查工具：pdftoppm 渲染页面再 Read png。
- 坑：Windows matplotlib 的 DateFormatter 不支持 %-m（用 %m月）；bash 无 grep/head/tail，xelaatex 输出重定向到文件后用 python 检查。

## §46 main.tex 按「问题一/问题二」素材重构（图片 + 附录代码 + 行文逻辑）
- **图片**：弃用 §45 的 matplotlib 直出 pdf，全部改用 问题一/图片、问题二/图片 的 10 张 png，复制到 CUMCM2026-Template/figures/ 并改名 p1_fig1-3、p2_fig1-7。语义（用户确认）：p1=输入特征/最优策略/敏感性；p2=框架/预测构成/指定日四联细节/全年逐日/月度累计/紧急购电时空分布/策略阶梯+条件消融。
- **附录**：用 `\usepackage{fancyvrb}` + `\VerbatimInput[frame=single,numbers=left,fontsize=\scriptsize,label=...]`。**minted 在本机不可用**（env 的 safe-delete 钩子会破坏 .minted 数据文件，报 OSError），fancyvrb 支持中文注释、无需 -shell-escape。附录只放核心函数：code/q1_core.py（89 行，solve_q1+validate）、code/q2_core.py（116 行，forecast_dow_N+solve_plan_lp+execute+settle）。注意 Verbatim 不接受 breaklines/breakanywhere。
- **结构**：问题二结果拆成「总—分—险—对照」（总费用构成 → 指定日表1/2/3 → 全年形态+月度 → 紧急与风险 → 策略对照与消融）；框架图前置到模型章开头而非结果章。Q3/Q4 仍留白。
- **编译**：27 页，0 error / 0 undefined ref / 0 overfull。修 overfull 三处：附录长标题改短、长数字串改写为「充入 X、放出 Y」句式；导言区加 `\emergencystretch=2em` 后彻底清零。xelatex 路径 `C:/texlive/2026/bin/windows/xelatex.exe`。
- 清理：删除根目录 _prep_assets.py、_fig_probe.py 与 _b*.log。

## §47 配图换成矢量 PDF + 问题三入正文 + 编译通过
- **figures/ 重建**：把 问题一/图片/fig1-3.pdf、问题二/图片/fig1-7.pdf、问题三/图片/fig1-6.pdf 复制进 CUMCM2026-Template/figures/，重命名为 **p1_fig1-3 / p2_fig1-7 / p3_fig1-6**（共 16 个）；原 10 张 png（p1_/p2_fig*.png）+ 6 张脚本直出 pdf（fig_q1_*/fig_q2_*）+ fig1_input_profile.pdf 全部移到 `_archive/figures_replaced/`（可逆）。main.tex 里 10 处 `[H]` 配图由 `.png` 改 `.pdf`。
- **PDF 裁白边（关键技巧）**：这些 matplotlib PDF 是整幅 A4 页（595×842pt）而内容只占中间一小块，直接 includegraphics 会让图缩得极小、页面大片留白。用 **PyMuPDF** 取 `get_drawings()/get_text('blocks')/get_image_info()` 的并集外接框，`set_cropbox()` 后另存 —— 等价 matplotlib `bbox_inches='tight'`。裁后宽高比与原 png 一致（p1_fig1 1.384 vs png 1.391 等），故沿用原 width 参数；Q3 的 4 张宽图（比 2.1）改 0.95–0.96\textwidth。PDF 从 A4 全页裁到内容框后全文 **48 → 37 → 35 页**。
- **问题三正文**（原留白 → 完整章节）：7.1 信息与结算变化 / 7.2 预报通道与净负载预测（整点插值积分 + 分档收缩回归 κ_g，K=30 因果）/ 7.3 调整层带双向违约价 LP（**§7.3 里给出"p·b′+0.5p(u+v)"与题面"下调 0.5p、超调 1.5p"的等价化归**）/ 7.4 2×2 费用矩阵表 / 7.5 指定日形态 / 7.6 是否需要其他时刻预报（否定结论）。数字全部只取自 问题三/图片 里读出的图注（A0=1395.70、A0K=1376.20、B0=1355.58、B1=1345.71、重叠 9.62、全融合 B3 瀑布 −2.39/+13.96/−1.98/+9.59、门控 1002 次分布 128/28/45/133 等、门控增费 6.08、逐月 B3−B1 热力图），**未编造任何数**。
- **同步改动**：摘要加问题三段并加长（初版溢出到第 2 页只剩"关键词"，已删约 3 行收敛回 1 页）；关键词加"滚动合同调整；双向违约电价"；问题重述 Q3 由占位改成正式重述；问题分析加 §问题三"信息增加与调整机会的价值辨析"；符号说明表加 τ/f_τ/b′/u,v/κ_g/λ 共 5 行；模型评价标题改"（问题一、二、三部分）"并加 2 条优点 + 2 条局限；支撑材料加 附件3、问题三/代码、问题三/图片。
- **修既存缺陷**：① 带圈数字 ①②③④ 与希腊 β 被 xeCJK 判为西文、主字体无字形 → PDF 里是空白。导言区加 `\xeCJKDeclareCharClass{CJK}{"2460 -> "2473, "0370 -> "03FF}` 解决（**同理可治任何 CJK 字体缺字的符号**）。② 命题结尾的字面 ∎ 改 `$\blacksquare$`。
- **修版式**：`[H]` 浮动体塞不下会把整页剩下部分留白（原 p31/p33 大片空白）→ Q3 的 6 张图改 `[htbp]`、符号说明表改 `[htbp]`。xelatex 连编 3 遍后 **35 页，0 error / 0 undefined ref / 0 missing char / 0 overfull**（用 pdftotext 按 \f 切页扫"稀疏页"验证）。
- **未决（重要）**：`results/` 下**没有 result3.xlsx，也没有问题三的计算脚本** —— 问题三只有 `问题三/代码/代码.md`（含 `...` 占位的草稿）和 6 张结果图，图是外部产出。因此论文 Q3 章节**没有**题面要求的"指定日期表1/表2/表3 格式结果"，支撑材料也只列了图与代码目录。若要补表需先实现并跑通 Q3 全链路。另：代码草稿里写 `ETA_C=ETA_D=0.95`，与附录1 的 0.90 冲突；因 A0 恰等于 Q2 主方案 1395.70（用 0.9），正文一律按 **0.90** 写，草稿那行视为笔误。
- 环境记录：本机 xelatex = `C:/texlive/2026/bin/windows/xelatex.exe`；pdftotext/pdftoppm/pdfcrop 同在 texlive bin；PyMuPDF 1.28.2 装在 managed python env。**bash 在本机连 mkdir/ls/head/tail/grep 都没有**（不只 dirname），文件操作一律走 Python 脚本。

