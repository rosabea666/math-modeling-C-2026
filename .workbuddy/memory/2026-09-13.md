# 2026-09-13

## 答案表按题目原表样式重排（论文排版）

**需求**：用户给出 `D:\09_Temp\C题_表格汇总.tex`（题目表 1–4 的原始 LaTeX），要求把 main.pdf 中「答案所使用的表格」换成该样式。澄清结果：范围＝**仅 5 张"对应题目表 N"的答案表**；版式＝**边框与行列布局一并对齐**。

**改动文件**（仅表格环境，正文一字未动）：
- `sections/04-problem1.tex`：`tab:q1-t1`（表3）、`tab:q1-t2`（表4）
- `sections/06-problem2-results.tex`：`tab:q2-t1`（表8）、`tab:q2-t2`（表9）、`tab:q2-t3`（表10）

**样式对照**：
- 旧：`booktabs` 三线表（`\toprule/\midrule/\bottomrule`），4/3 列窄表 + `\resizebox` 宽表。
- 新：`|c|c|...` 全框线 + 全部 `\hline`；行排布照题目原表——
  - 表1：`时间段|购电量` ×3 并排，两行时段（10/12/14 与 16/18/20），末行「全天购电量｜值｜全天购电费｜值」（标签跨 2 列，值在下一列）。
  - 表2：`时间段|充电量|放电量` ×2，三行时段，末行「0:00 储电量｜值｜24:00 储电量｜值」。
  - 表3：4 个日期组「时间段|购电量」×4 并排 + 日期合并表头 + 3 行数据。
- Q2 含 4 个指定日期，按题目要求（"按表 1、表 2、表 3 的格式给出表 3 中指定日期的结果"）改为**每个日期一个块、纵向堆叠**：表8 21 行、表9 17 行；表10 用表3 的 8 列并排式。
- 单位文本保留：表8/表9 表头不重复带单位（题注已注明），表9 表头用 `\shortstack{紧急购电量\\(kWh)}` 控制列宽，表10 用 `\footnotesize`+`tabcolsep 3pt`+`\resizebox`。
- 表3/表4（Q1）用 `\small`+`tabcolsep 4pt`；表8/表9 用 `\small`+`tabcolsep 4pt`。

**浮动参数选择（踩过一圈）**：
- `[htbp]`：LaTeX 会把 21 行的表8 放成**居中浮动页**（上下约 40% 空白），且**第 45 页只剩一行正文**（"月末的价格效应。"）。
- `[!ht]`：53 页，仍残留第 45 页单行页。
- `[H]`（float 宏包，仅用于表8/表9/表10）：**54 页，无单行页**，第 19 页是含图6 的图页（正常）。**最终采用 `[H]`**。Q1 两张表保持 `[htbp]`（第 11 页排布良好）。
- 试过放宽 `\topfraction` 等（0.92/0.85/0.06/0.88）→ 不但没让表8+表9 同页，还引入 1 处 Overfull，**已回退**，preamble 未改。

**验证**（均通过）：
- `latexmk main.tex`：54 页，**0 Overfull / 0 Underfull / 0 Missing character / 0 LaTeX Warning**。
- 逐页字符数扫描：稀疏页 18/19/22/29/32/54 全部含矢量图（p19 为图6 页），无空白页。
- **文字未改动**：把旧 PDF 所有 ≥25 字符的长行（去空白）逐条在新 PDF 全文（去空白）中查找，**0 条缺失**；反向多出的 5 条全是表格内同一行拼接（如 `16:00–16:10 512.0462 18:00–18:10`），属预期。
- 逐表目视复核：p11（表3、表4）、p20（表8）、p21（表9、表10）渲染正确，数值与旧版一一对应。

**备份**：`_refactor_backup/{04-problem1.tex,06-problem2-results.tex,main.pdf}.0913-1122.bak`。

**副产品**：`sections/*.tex` 中已无 `\multirow` 使用（原仅 tab:q2-t3 用），宏包仍由 sty 加载，无副作用。

---

## 按《初版论文修改建议》做**纯文本修订**（不动图、不重算）

**来源**：`D:\01_Personal\Documents\xwechat_files\...\初版论文修改建议.md`（13 节 + 10 项必改清单）。
**用户定范围**：只改文本（摘要/重述/分析/假设/符号/公式/账本/结论表述/附录清单/AI声明），**不动图、不动结果文件、不新算数**；AI 声明由用户提供事实清单。

### 公式与账本（必改项，全部落实）
- **Q2 分位裕量**改为与代码一致：`M^(β)_{d,t}=Q_β{N_{s,t}−m_{d,t}}`，中心化基准是**同一窗口**中位数、**无 max{·,0} 截断**；拆成全量符号 $\mu$（点预测）/$\widetilde N$（规划需求代理）。
- **报童临界分位数**：删掉错误的 `5p/(5p+p)=0.83`，改 `F_N(b*)=0.8`（欠购净边际损失 4p、过购 p）。
- **执行器**改为显式 must/max：$c_t=\min\{[\mathrm{net}]^+,\bar C,(E_{max}-E_t)/\eta_c\}$ 等；删掉"计划充电意图"作为执行上限的旧写法。
- **Q2 冷启动**：函数级核查证明零向量分支**确实被调用**（5 次，$d=30..34$ 即 1/31–2/4，候选日 $d'=2..6$），原文"不会使用该零向量"是错的，已改为如实说明。
- **Q4 账本**：改为 $J=\sum p_t b^{\rm f}_t+\frac12\sum p_t|b^{\rm f}-b^{0}|+5\sum p_t e_t$，并给出等价的"初始计划费+调整相关费+紧急费"式；明确净额性质、明确该式**已含 1.5p 调增边际成本**（原文"Q4 不引入 1.5p"是错的，已删）。
- **Q4 PRM**：$\widetilde N'_t=\mu_t+\omega_t M^{(\beta+\delta)}$（原式在 $\lambda_p=0$ 时会让 $\delta$ 失效）；权重记 $\omega_t$ 与余量 $w_t$ 区分。
- **Q4 价格前缀**：分子改 $\sum\pi P+\zeta\bar\ell$，分母 $\sum\pi^2+\zeta$。
- **缩放命题**：补"适用范围（不可外推之处）"三点（只对全时域同比例成立；不能推出 $\ell_d$ 无价值；不涉及预测误差传导）。

### 复算出来的新证据（写进论文）
- 附件4 逐区间均值 vs 附件1 TOU：corr=0.999999996，**最大绝对偏差 5.15e-5 元/kWh**（原只写"相关 1.0000"）。
- $\ell_d$（回归系数口径）**std=0.129**，不是图内/原文的 0.077（原文与自身区间 [0.7199,1.2047] 矛盾）；残差 std=0.081。
- 版本：Python 3.13.14 / NumPy 2.5.2 / SciPy 1.18.1 / pandas 3.0.5 / openpyxl 3.1.5。

### 结论口径收紧
- 删"其他时刻预报得到否定结论"→"在所比较的融合与门控策略中未进一步降低费用"；删"48h 合计降费 17.78 万"→分分支报 11.08 / 6.70 万（表 tab:q4-horizon 的"合计"行已删）。
- 删"价格预知上限/真实信息价值/算力应投向负荷侧"；7.7030 定位为"24h+G_fixed 固定策略下替换价格输入的条件差额"，不得分配到 G_48 与 1277.92 的 126.32 万差距。
- "严格下界"→**放松下界**（唯一保留的"严格下界"是否定用法）；"次日全知上界"→**次日完美信息参照**。
- 终端归一化：写"预测末端一致、回放后配对方案实际终端相同"（Q2 分支 10800.00、Q3 分支 10623.46）。
- 日循环：改"限制的是计划中的单日净库存变化"，删"禁止跨午夜持电"。
- Q1 边际价值：注明"固定下限、只抬升上限"路径 + 差分步长 h∈{60,120,480,1200} + 不得乘 365。

### 结构/附录
- 摘要压到**整 1 页**（关键词 5 个）；问题重述压成"信息/权限/难点"三列表；问题分析每问三环节。
- 假设拆三类（题面给定 / 解释与近似 / 算法与评价设计）；符号表加 μ/Ñ/b⁰/bᶠ/Ê/ω/λ_p/T/H。
- 表8 拆成"指定区间购电量（日期作列）+ 日汇总"两张；表10 紧急购电改竖表、去掉 resizebox。
- 附录重写：五工作簿+入口表、脚本清单（每问单一入口）、环境版本、**论文图表↔数据↔脚本映射表**、六项审计分项；references 补 Hyndman fpp3 §5.10、Mayne 2000 Automatica 36(6):789-814。
- 新增 `backmatter/declaration.tex`（AI 使用声明，**5 处【待填】占位**），在 references 之前 `\input`。

### 编译状态
`latexmk -xelatex`：**60 页，0 Overfull / 0 Underfull / 0 undefined / 0 missing char**；摘要正好 1 页；正文 p2–p48（**47 页**）、声明 p49、参考文献 p50、附录 p50–60。
排版坑：`tabularx` 里中文长列必须走 `X`（我曾误用 `l` → 31pt Overfull + "Float too large"）；X 列用 `>{\raggedright\arraybackslash}`；附录脚本清单含大量 `\texttt` 长路径，改 `\footnotesize` 后 underfull 归零。

### 未做（用户选定范围外）
- 正文 47 页 > 建议的 30 页：需把约 8–9 幅次要图移到附录（用户本次选了"不动图"）。
- Q3 指定日期四表 + `result3.xlsx`：工作区无 Q3 可跑链路（`问题三\代码\代码.md` 是带 `...` 占位的草稿，且其中 ETA=0.95 与论文 0.9 矛盾）。
- 图17 图内 σ=0.077、图19 虚线基准、图20 "上界"字样的**图内标注**无法改（只在图注/正文写明以复核值为准）。
